# 4.3 方法 / 模型设计（第4章“核心篇幅”）

## 4.3.1 方法总体框架概览

本方法面向 SEED-IV 原始 EEG，构建端到端情感识别模型。整体流程如下：

```mermaid
flowchart LR
    A[输入: 单个 trial 原始 EEG<br/>形状 C×T, C=62] --> B[非重叠切窗<br/>W×C×800, 4s@200Hz]
    B --> C[可学习 Sinc 滤波器组<br/>5 频带]
    C --> D[DE-like 特征<br/>按通道-频带取 log-variance]
    D --> E[特征序列<br/>W×310]
    E --> F[TCN 平滑模块<br/>沿窗口轴建模]
    F --> G[线性投影<br/>310→d_model]
    G --> H[Conformer 编码器×L]
    H --> I[Mask Mean Pooling]
    I --> J[分类头<br/>4 类情感]
```

相对同仓库基线（`seed_iv_2026`）的关键改动如下。

1. 输入层改动：由“离线 `de_LDS` 特征输入”改为“原始 EEG 端到端输入”。
2. 特征构建改动：引入可学习 Sinc 滤波器组与 DE-like 映射，替代固定特征提取流水线。
3. 时序增强改动：在编码器前加入 TCN 平滑模块，以模拟并替代传统 LDS 的时序平滑作用。
4. 主干网络选择：保留 Conformer 作为主干，利用“全局注意力 + 局部卷积”联合建模跨窗时序依赖。

上述改动的核心目标是：在保留 DE/LDS 先验解释性的同时，将频带划分与时序平滑参数化、可学习化，减少手工特征与任务目标间的优化割裂。

## 4.3.2 关键模块1：特征构建 / 嵌入 / 预处理

### (1) 数据组织与切窗

每个样本单位为一个 trial。原始 trial 先对齐为 `(C, T)`，其中 `C=62`。随后按固定窗长 `chunk_size=800`（4 秒）进行非重叠切窗，得到 `(W, C, 800)`。其中：

- `W = floor(T / 800)`；
- 末尾不足 800 点的数据被截断，不参与训练；
- 不同 trial 的 `W` 可变，batch 内通过 padding 对齐并记录真实长度 `lengths`。

### (2) 可学习 Sinc 滤波器组

为解决固定频带边界难以适配个体差异的问题，引入 SincNet 风格带通滤波器组。初始频带设置为：

- `[(1,4), (4,8), (8,14), (14,31), (31,50)] Hz`。

每个频带的上下截止频率通过可学习参数更新，并通过 softplus 约束保持频率有效性。第 `b` 个滤波器核形式为：

\[
h_b[n] = \left(2f_{2,b}\mathrm{sinc}(2f_{2,b}n) - 2f_{1,b}\mathrm{sinc}(2f_{1,b}n)\right)w[n]
\]

其中 \(w[n]\) 为汉明窗。该设计可在“频带可解释性”与“可学习性”之间取得平衡。

### (3) DE-like 映射

为对齐传统 DE 特征思想，对滤波后信号按“通道-频带”计算对数方差：

\[
\mathrm{DE\_like} = \log(\mathrm{Var}(x)+\epsilon)
\]

对每个窗口输出 `62×5=310` 维特征，即将 `(W, C, B, T)` 映射为 `(W, 310)`。  
该映射能稳定刻画能量分布，并降低原始波形的相位敏感性。

### (4) 标准化策略

训练前可选按 trial 做“按通道 z-score”（代码默认开启，可通过 `--no_zscore` 关闭）：

\[
\hat{x} = \frac{x-\mu_{c}}{\sigma_{c}+10^{-6}}
\]

其中均值与标准差在“窗口轴+时间轴”上统计。该策略用于缓解跨被试振幅尺度差异，提高优化稳定性。

## 4.3.3 关键模块2：主体网络 / 增强策略

### (1) TCN 平滑模块（前端增强）

为解决窗口级特征抖动与局部时序噪声问题，引入 TCN 平滑模块。每层由以下结构组成：

1. 深度可分离一维卷积（沿窗口轴）；
2. GELU 非线性与 Dropout；
3. `1×1` 点卷积重组通道；
4. 残差连接；
5. LayerNorm。

相比直接将 DE-like 特征送入编码器，该模块能够在较低计算代价下先完成局部平滑，减轻主干网络对短时噪声的建模负担。

### (2) Conformer 主干编码器

平滑后的 `W×310` 特征先线性投影到 `d_model`，再输入多层 Conformer。Conformer 同时包含：

1. 多头自注意力：建模跨窗口长程依赖；
2. 卷积分支：强化局部动态模式与邻域相关；
3. 前馈网络与残差归一化：提升非线性表达稳定性。

为解决变长序列 padding 对统计的干扰，编码器接收 `lengths` 掩码；输出阶段采用 mask-aware mean pooling 聚合有效窗口。

### (3) 分类头与输出

池化后特征经 `LayerNorm + Linear` 输出 4 类 logits，使用 softmax 解释为类别概率。

### (4) 设计动机总结（可直接用于论文行文）

- 为解决“手工频带不适配个体差异”，引入可学习 Sinc 滤波器以增强频域自适应能力。
- 为解决“窗口级特征不平滑”，引入 TCN 平滑模块以增强局部时序鲁棒性。
- 相比纯 Transformer，Conformer 额外引入卷积路径，能够同时兼顾全局依赖与局部结构保持，更适合 EEG 的多尺度时序模式。

## 4.3.4 关键模块3：损失函数 / 训练目标 / 评价指标

### (1) 训练目标

当前实现采用标准四分类交叉熵损失：

\[
\mathcal{L}_{CE} = -\frac{1}{N}\sum_{i=1}^{N}\sum_{c=1}^{4}y_{i,c}\log p_{i,c}
\]

其中 \(p_{i,c}\) 为样本 \(i\) 属于第 \(c\) 类的预测概率。

### (2) 优化策略

- 优化器：AdamW；
- 可选学习率调度：CosineAnnealingLR（默认关闭）；
- 可选混合精度训练 AMP（默认关闭）；
- 梯度裁剪：`max_norm=5.0`；
- 早停依据：`val_acc`，当提升小于 `early_stop_min_delta` 且连续 `early_stop_patience` 轮不提升时停止。

### (3) 评价指标

代码默认指标为 Accuracy：

\[
\mathrm{Acc} = \frac{\#\text{correct}}{\#\text{all}}
\]

单 fold 记录 `best_val_acc`、`test_acc`、`best_epoch`；完整 LOSO 额外汇总 `mean±std`。  
若论文需要更细粒度类别平衡分析，可在实验节补充 Macro-F1（非当前脚本默认输出）。

# 4.4 实验设计与实现细节

## 4.4.1 实验数据 / 数据集介绍

实验使用 SEED-IV 原始 EEG 数据，目录组织为 `session 1/2/3`。根据当前数据检查结果：

1. 每个 session 含 15 个被试文件（共 45 个 `.mat`）；
2. 每个被试文件含 24 个 trial EEG 键；
3. 每个 trial 使用 62 通道 EEG；
4. 任务为 4 类情感识别。

因此全数据规模为 `15 × 3 × 24 = 1080` 个 trial。

划分协议采用严格被试独立 LOSO：

1. 每个 fold 留 1 名被试做测试；
2. 其余 14 名被试用于训练/验证；
3. 验证集仅从训练被试内部按 trial 划分（默认 `val_split=0.2`），测试被试全程不参与选模。

按默认 `val_split=0.2`，每个“被试-会话”24 个 trial 中约 5 个用于验证、19 个用于训练；因此每 fold 约为：

1. 训练 trial：`14 × 3 × 19 = 798`；
2. 验证 trial：`14 × 3 × 5 = 210`；
3. 测试 trial：`1 × 3 × 24 = 72`。

## 4.4.2 标注规范 / 预处理策略

### (1) 标注规范

标签由每个 session 的固定 `LABELS_SEED4` 映射给出，trial 编号 `1..24` 与标签一一对应。数据加载时会强校验：

1. 每个文件 trial 编号完整覆盖 `1..24`；
2. 试次键名符合 `*_eeg{trial_id}`；
3. trial 维度可对齐到 `62×T`。

### (2) 预处理策略

1. 非重叠切窗：`chunk_size=800`（4 秒）；
2. 可选按 trial 的按通道 z-score；
3. batch 级补零 + 长度掩码，支持变长窗口序列；
4. 可选磁盘缓存（`.npz`）加速重复读取。

该预处理遵循“最小手工处理”原则，不进行额外数据增强，以突出模型结构本身贡献。

## 4.4.3 实验设置

### (1) 训练环境

代码基于 PyTorch；设备自动选择 `cuda`（可用时）否则 `cpu`。随机种子默认为 `42`。

### (2) 默认超参数

| 参数 | 默认值 |
|---|---|
| sessions | `[1,2,3]` |
| chunk_size | `800` |
| num_channel | `62` |
| batch_size | `8` |
| epochs | `80` |
| lr | `2e-4` |
| weight_decay | `1e-2` |
| dropout | `0.1` |
| grad_clip | `5.0` |
| d_model | `256` |
| num_heads | `4` |
| ffn_dim | `1024` |
| num_layers | `6` |
| conformer_conv_kernel | `15` |
| num_workers | `4` |
| val_split | `0.2` |
| early_stop_patience | `10` |
| early_stop_min_delta | `0.001` |

### (3) 日志与可复现输出

1. 按 fold 输出 checkpoint（可选 `--save_dir`）；
2. CSV 记录字段包括 `best_val_acc`、`test_acc`、`best_epoch`、运行时间等；
3. 完整 LOSO 输出各 fold 精度及均值、标准差。

## 4.4.4 评价指标

本任务为四分类，主指标使用 Accuracy。实验报告建议按以下层级给出：

1. 单 fold：`best_val_acc` 与 `test_acc`；
2. 全部 LOSO：`mean_test_acc ± std_test_acc`；
3. 可选补充：Macro-F1（用于类别不均衡敏感场景）。

对应地，模型选择严格依据验证集 Accuracy，测试集仅在选模完成后评估一次，避免数据泄露。

